apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: monitoring

helmCharts:
  - name: loki
    repo: ghcr.io/grafana-community/helm-charts/loki
    version: "17.1.1"
    releaseName: loki
    namespace: monitoring
    valuesFile: values.yaml
